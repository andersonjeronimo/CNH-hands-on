export enum Status {
    Ativo = "active",
    Inativo = "inactive",
    Pausado = "paused"
}

export enum Category {
    A = "a",
    B = "b",
    AB = "both"
}

export enum Vehicle { 
    Proprio = "owned",
    Aluno = "student",
    Ambos = "both"
}