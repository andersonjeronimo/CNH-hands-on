export enum Status {
    Ativo = "active",
    Inativo = "inactive",
    Pausado = "paused"
}

export enum Category {
    A = "a",
    B = "b",
    AB = "a/b"
}

export enum Vehicle { 
    Proprio = "owned",
    Nao = "borrowed"
}